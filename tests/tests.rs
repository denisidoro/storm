#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        //let _x = storm::handle_config(storm::config_from_iter(
        //"storm best trivial".split(' ').collect(),
        //));
        // assert_eq!(x, 3);
    }
}
